export default function AdminPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-zinc-50 px-6 py-16 text-zinc-950">
      <section className="w-full max-w-xl space-y-4">
        <p className="text-sm font-medium uppercase tracking-wide text-zinc-500">Compre Junto</p>
        <h1 className="text-3xl font-semibold tracking-tight">Loja conectada</h1>
        <p className="text-base leading-7 text-zinc-600">
          A instalação foi concluída. A base do app já pode reconhecer a loja e preparar as próximas
          etapas do painel.
        </p>
      </section>
    </main>
  );
}
